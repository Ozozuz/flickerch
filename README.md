# flickerch
Flickerch is a full stack Android application that rely on Flickr's API to give the users the possibility to view, dowload and search images inside the flickr platform.
The backend is entirley written in NodeJS ( a first time also here ) and rely on FireBase / FireStore for autentication and storing datas.
